{
  "name": "WashswatExample",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "npx react-native run-android",
    "android-clean": "cd ./android && ./gradlew clean && cd ..",
    "yarnstart": "yarn start -- --reset-cache",
    "reinstall": "rm -rf node_modules/ && rm -rf yarn.lock && yarn install",
    "podinstall": "cd ios && rm -rf Podfile.lock && rm -rf Pods && pod install && cd ..",
    "fix-image": "npx react-native-fix-image",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "17.0.1",
    "react-native": "0.64.0"
  },
  "devDependencies": {
    "@babel/core": "^7.13.15",
    "@babel/runtime": "^7.13.10",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "eslint": "^7.24.0",
    "jest": "^26.6.3",
    "metro-react-native-babel-preset": "^0.65.2",
    "react-test-renderer": "17.0.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
